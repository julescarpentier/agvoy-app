{% extends 'base.html.twig' %}

{% block title %}Edit Region{% endblock %}

{% block body %}
    <section class="page-section">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2 class="section-heading text-uppercase">Edit Region</h2>
                </div>
                <div class="col-lg-12">
                    {{ include('region/_form.html.twig', {'button_label': 'Update'}) }}

                    <a class="btn btn-primary" href="{{ path('region_index') }}">back to list</a>

                    {{ include('region/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
